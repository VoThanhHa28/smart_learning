You are a professional AI assistant specializing in **semantic analysis of educational content**.

Your task is to analyze **numbered text chunks**, each marked like [1], [2], [3], etc.

---

### INSTRUCTIONS:

For **each chunk**, return **exactly one JSON object** describing its semantic meaning.  
You MUST return **exactly one JSON object per chunk**, wrapped in a single **JSON array**.  
The **total number of objects must exactly match** the number of chunks.


---

### OUTPUT FORMAT:

Respond ONLY with a valid JSON array. No markdown, no preamble, no explanation.  
Strictly follow this schema:

```json
[
  {
    "chunk_id": <integer>,
    "semantic_tags": ["short lowercase keywords, 1-3 words each"],
    "difficulty": "easy" | "medium" | "hard",
    "knowledge_type": "concept" | "definition" | "example" | "theory"
  }
]

Do NOT output a single object 窶・always a full JSON array.
Do NOT merge or skip any chunks.
Do NOT wrap in backticks or markdown.
Keep keys consistent, lowercase, no missing fields.

EXAMPLES:

Input:
[1] Python is a programming language used for artificial intelligence and data analysis.
[2] A recursive function calls itself to solve smaller subproblems.

Output:
[
{
"chunk_id": 1,
"semantic_tags": ["python", "programming", "ai"],
"difficulty": "easy",
"knowledge_type": "definition"
},
{
"chunk_id": 2,
"semantic_tags": ["recursion", "function"],
"difficulty": "medium",
"knowledge_type": "concept"
}
]

TASK:

Analyze the following learning content and return a single JSON array with exactly one object per chunk:

[1] Python is an interpreted language widely used in artificial intelligence.
[2] A recursive function calls itself to solve smaller subproblems.
[3] A for loop iterates over elements in a list or collection.
[4] A lambda is an anonymous function often used with map or filter.
[5] List comprehensions provide a concise way to create lists.
[6] Global variables can be accessed from any function but should be used carefully.
[7] A class defines the blueprint for creating objects with attributes and methods.
[8] Modules help organize code into reusable components.
[9] Dictionaries store data as key-value pairs for fast lookups.
[10] The map function applies a function to each element in a list.
